// Babel config for standalone Jest (not react-scripts)
// react-scripts already installs these babel packages
module.exports = {
  presets: [
    ['@babel/preset-env', { targets: { node: 'current' } }],
    ['@babel/preset-react', { runtime: 'automatic' }]
  ]
};
